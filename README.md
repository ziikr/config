# Ziikr config package

## Installation

To install via composer: 

```bash
composer require "ziikr/config"
```

Next, you should run the `ziikr:config.install` command, it will create the config tables.

```shell
// install
php artisan ziikr:config.install
```

